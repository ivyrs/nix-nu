# vim: ft=bash

set -euo pipefail

JOURNAL_DIR="${JOURNAL_DIR:-$HOME/johnny-decimal/10-19 Life admin/16 Journal}"

mkdir -p "$JOURNAL_DIR"

date="$(date +%F)"
entry="$JOURNAL_DIR/$date.md"

if [[ ! -e "$entry" ]]; then
  cat >"$entry" <<EOF
# $(date '+%A, %d %B %Y')


EOF
fi

exec nvim "$entry"
